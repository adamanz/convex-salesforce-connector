# =============================================================================
# CONVEX SALESFORCE CONNECTOR - ENVIRONMENT VARIABLES
# =============================================================================

# -----------------------------------------------------------------------------
# OPTION 1: Simple Auth (Session Token)
# Use this for quick testing. Token expires every 2 hours.
# -----------------------------------------------------------------------------

# Get from: sf org display -o YourOrg --json | jq -r '.result.accessToken'
SALESFORCE_ACCESS_TOKEN=

# Your Salesforce instance URL (e.g., https://your-org.my.salesforce.com)
SALESFORCE_INSTANCE_URL=

# -----------------------------------------------------------------------------
# OPTION 2: Connected App OAuth (Recommended for Production)
# Tokens auto-refresh. Requires Connected App setup in Salesforce.
# -----------------------------------------------------------------------------

# From your Connected App settings
SALESFORCE_CLIENT_ID=
SALESFORCE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# WEBHOOK SECURITY
# -----------------------------------------------------------------------------

# Shared secret for HMAC signature verification
# Generate with: openssl rand -hex 32
# Must match the secret configured in Salesforce Custom Metadata
SALESFORCE_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS
# -----------------------------------------------------------------------------
#
# For Simple Auth:
# 1. Run: sf org login web -a YourOrg
# 2. Run: sf org display -o YourOrg --json
# 3. Copy accessToken and instanceUrl above
#
# For OAuth (Recommended):
# 1. In Salesforce Setup, go to App Manager
# 2. Create a new Connected App:
#    - Enable OAuth Settings
#    - Callback URL: https://YOUR_CONVEX_URL/oauth/salesforce/callback
#    - Scopes: Full access (full), Perform requests at any time (refresh_token)
# 3. Copy Consumer Key and Secret above
# 4. Run the setup wizard: npm run setup
#
